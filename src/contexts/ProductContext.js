import { createContext } from 'react'


const ProductContext = createContext({
    //unsure that this is the structure I'm actually putting here, might just be putting an array of objects commenting out until sure (this is just for documentation clarity anyway)
    // id: "",
    // title: "",
    // price: "",
    // image: ""
})

export default ProductContext;